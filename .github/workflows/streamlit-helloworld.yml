name: Deploy Streamlit to Azure

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write   # for GHCR
      id-token: write   # for Azure OIDC login

    steps:
      # 1️⃣ Checkout code
      - name: Checkout repo
        uses: actions/checkout@v4

      # 2️⃣ Login to GitHub Container Registry (GHCR)
      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      # 3️⃣ Build Docker image
      - name: Build Docker image
        run: |
          docker build -t ghcr.io/matthewengineering/streamlit-helloworld:latest \
                       -t ghcr.io/matthewengineering/streamlit-helloworld:${{ github.sha }} .

      # 4️⃣ Push Docker image to GHCR
      - name: Push Docker image
        run: |
          docker push ghcr.io/matthewengineering/streamlit-helloworld:latest
          docker push ghcr.io/matthewengineering/streamlit-helloworld:${{ github.sha }}

      # 5️⃣ Login to Azure
      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.STREAMLITHELLOWORLD_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.STREAMLITHELLOWORLD_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.STREAMLITHELLOWORLD_AZURE_SUBSCRIPTION_ID }}

      # 6️⃣ Deploy to Azure Container Apps
      - name: Deploy to Azure Container Apps
        uses: azure/container-apps-deploy-action@v2
        with:
          containerAppName: streamlit-helloworld
          resourceGroup: myResourceGroup
          imageToDeploy: ghcr.io/matthewengineering/streamlit-helloworld:${{ github.sha }}
          ingress: external
          targetPort: 8501